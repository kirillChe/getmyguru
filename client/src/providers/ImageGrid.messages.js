import { defineMessages } from 'react-intl';

export default defineMessages({
    userNotLoggedIn: {
        id: 'imageGridProvider.userNotLoggedIn',
        defaultMessage: '_Only logged in users can watch profiles_'
    },
});
